# Backend: cli | mock | auto (default: auto)
PLANNER_BACKEND=auto

# Claude model to use (informational; CLI uses its own model config)
PLANNER_MODEL=claude-sonnet-4-6

# Port for the FastAPI server
PORT=8200
