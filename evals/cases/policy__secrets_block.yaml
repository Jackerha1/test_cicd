# Independent regression test for the secrets_block rule. Touching .env*
# must always block, regardless of any other rule changes.

name: policy__secrets_block
target: policy_engine
event: examples/pr_secrets_attempt.json
expect:
  rule_id:    secrets_block
  risk_level: critical
  block:      true
